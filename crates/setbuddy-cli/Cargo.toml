[package]
name = "setbuddy-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "setbuddy"
path = "src/main.rs"

[dependencies]
setbuddy-core.workspace = true
setbuddy-engine.workspace = true
setbuddy-mpv.workspace = true
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[features]
# End-to-end tests that spawn a real mpv.
integration = []
